{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://orionai-dev.github.io/mcp-secure-context-sharing/schemas/astrospec/defs-0.1.json",
  "title": "AstroSpec Shared Definitions",
  "type": "object",
  "$defs": {
    "source": {
      "type": "string",
      "enum": ["user", "context", "default", "memory", "model", "system"]
    },
    "fieldScope": {
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind", "value"],
          "properties": {
            "kind": { "const": "filetype" },
            "value": { "type": "string", "minLength": 1 }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind", "id"],
          "properties": {
            "kind": { "const": "project" },
            "id": { "type": "string", "minLength": 1 }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind", "value"],
          "properties": {
            "kind": { "const": "intent" },
            "value": { "type": "string", "minLength": 1 }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind"],
          "properties": {
            "kind": { "const": "global" }
          }
        }
      ]
    },
    "evidenceSpan": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "page": { "type": "number", "minimum": 0 },
        "section": { "type": "string" },
        "start": { "type": "number", "minimum": 0 },
        "end": { "type": "number", "minimum": 0 },
        "locator": { "type": "string" }
      }
    },
    "evidenceRetrieval": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "technique": { "type": "string" },
        "stage": { "type": "string" },
        "score": { "type": "number" },
        "rank": { "type": "number", "minimum": 0 }
      }
    },
    "evidenceRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ref"],
      "properties": {
        "ref": {
          "type": "string",
          "description": "File path, URL, or opaque reference."
        },
        "kind": {
          "type": "string",
          "description": "Free-form kind (test, doc, artifact, bdd, log, ...)."
        },
        "selector": {
          "type": "string",
          "description": "Free-form selector; integrations define semantics."
        },
        "checksum": {
          "type": "string",
          "description": "Integrity hint (recommended format: sha256:<hex>)."
        },
        "sourceId": {
          "type": "string",
          "description": "Stable source identifier when a retrieval system assigns one."
        },
        "span": {
          "$ref": "#/$defs/evidenceSpan"
        },
        "retrieval": {
          "$ref": "#/$defs/evidenceRetrieval"
        },
        "ext": {
          "type": "object",
          "additionalProperties": true
        }
      }
    },
    "specField": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["string", "number", "boolean", "enum", "object", "array", "any"]
        },
        "value": {},
        "default": {},
        "required": { "type": "boolean" },
        "min": { "type": "number" },
        "max": { "type": "number" },
        "enum": { "type": "array", "items": {} },
        "pattern": { "type": "string" },
        "many": { "type": "boolean" },
        "noneAllowed": { "type": "boolean" },
        "description": { "type": "string" },
        "source": { "$ref": "#/$defs/source" },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "rationale": { "type": "string" },
        "scope": { "$ref": "#/$defs/fieldScope" },
        "ext": { "type": "object", "additionalProperties": true },
        "properties": {
          "type": "object",
          "description": "For object fields only.",
          "additionalProperties": { "$ref": "#/$defs/specField" }
        },
        "items": {
          "description": "For array fields only.",
          "$ref": "#/$defs/specField"
        }
      }
    }
  }
}
