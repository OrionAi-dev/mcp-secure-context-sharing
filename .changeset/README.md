This directory is managed by Changesets.

Use `pnpm changeset` to describe any user-visible package change that should be versioned and released.

Do not commit ad hoc version bumps outside the Changesets workflow.
